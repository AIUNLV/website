/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    // Add domains as needed when switching to next/image
    remotePatterns: [],
  },
};

export default nextConfig;
